const { Client } = require("pg");

const connectionString = "postgres://postgres:Rafimn21041999.@db.eehktxhhpsdffpwlxghm.supabase.co:5432/postgres";

const sql = `
CREATE TABLE IF NOT EXISTS guest_comments (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  invitation_id TEXT REFERENCES invitations(id) ON DELETE CASCADE NOT NULL,
  name TEXT NOT NULL,
  comment TEXT NOT NULL,
  rsvp_status TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_guest_comments_invitation_id ON guest_comments(invitation_id);
`;

async function main() {
  console.log("Connecting to Supabase PostgreSQL database to create guest_comments table...");
  const client = new Client({
    connectionString: connectionString,
    ssl: {
      rejectUnauthorized: false
    }
  });

  try {
    await client.connect();
    console.log("Connected successfully!");
    console.log("Running SQL script...");
    await client.query(sql);
    console.log("guest_comments table and index created successfully!");
  } catch (error) {
    console.error("Failed to run SQL query:", error);
  } finally {
    await client.end();
  }
}

main();
